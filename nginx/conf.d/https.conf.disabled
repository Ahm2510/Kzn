# Enable HTTPS by renaming this file to https.conf and providing Let's Encrypt certs.
# This file is disabled by default to avoid nginx startup failures when certs are missing.
#
# Expected cert paths inside the container:
#   /etc/letsencrypt/live/<your-domain>/fullchain.pem
#   /etc/letsencrypt/live/<your-domain>/privkey.pem
#
# server {
#   listen 443 ssl http2;
#   server_name your-domain.com;
#
#   ssl_certificate     /etc/letsencrypt/live/your-domain.com/fullchain.pem;
#   ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
#
#   root /usr/share/nginx/html;
#   index index.html;
#
#   location /api/ {
#     proxy_pass http://service_a:8002/;
#     proxy_set_header Host $host;
#     proxy_set_header X-Real-IP $remote_addr;
#     proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     proxy_set_header X-Forwarded-Proto https;
#   }
#
#   location / {
#     try_files $uri $uri/ /index.html;
#   }
# }
